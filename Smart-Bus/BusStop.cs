using System;
using Microsoft.SPOT;

namespace Smart_Bus
{
    public struct BusStop
    {
        public int id;

        public BusStop(int id)
        {
            this.id = id;
        }
    }
}
